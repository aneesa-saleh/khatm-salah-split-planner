export interface ScheduleForm {
    rangeStart: number
    rangeEnd: number
    tahajjudChecked: boolean
    fajrChecked: boolean
    zuhrChecked: boolean
    asrChecked: boolean
    maghribChecked: boolean
    ishaChecked: boolean
    daysToComplete: string
}