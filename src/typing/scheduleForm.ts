export interface ScheduleForm {
    startRange: number
    endRange: number
    tahajjudChecked: boolean
    fajrChecked: boolean
    zuhrChecked: boolean
    asrChecked: boolean
    maghribChecked: boolean
    ishaChecked: boolean
    daysToComplete: string
}